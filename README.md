# telegram-inline-memegen-bot

Uses Imgflip's API to generate memes within inline Telegram messages!

config_template.json is included, just rename it to config.json and add your info to host it yourself!